version: '3.8'

services:
  api:
    build: .
    container_name: smart-grid-api
    ports:
      - "8000:8000"

  redis:
    image: redis:latest
    container_name: redis-server
    ports:
      - "6379:6379"